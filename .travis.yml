sudo: required
services:
  - docker

before_install:
  - docker build -t jonathansgardner/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jonathansgardner/docker-react npm run test
